<Window x:Class="FloatTodo.App.QuickTaskDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="任务详情"
        Width="420"
        Height="360"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ResizeMode="CanResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   FontSize="18"
                   FontWeight="Bold"
                   TextWrapping="Wrap"/>
        <TextBlock x:Name="PriorityText"
                   Grid.Row="1"
                   Margin="0,10,0,0"/>
        <TextBlock x:Name="DueTimeText"
                   Grid.Row="2"
                   Margin="0,6,0,0"/>
        <TextBlock x:Name="ProjectText"
                   Grid.Row="3"
                   Margin="0,6,0,0"/>
        <TextBlock x:Name="StatusText"
                   Grid.Row="4"
                   Margin="0,6,0,10"/>

        <TextBox x:Name="DescriptionText"
                 Grid.Row="5"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Padding="8"/>

        <!-- 详情窗口可作为截止提醒的处理入口：普通任务/项目小任务显示完成和删除，项目父节点保持只读。 -->
        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="CompleteButton"
                    Content="完成"
                    Width="80"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="CompleteButton_Click"/>
            <Button x:Name="DeleteButton"
                    Content="删除"
                    Width="80"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="DeleteButton_Click"/>
            <Button Content="关闭"
                    Width="80"
                    Height="30"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
