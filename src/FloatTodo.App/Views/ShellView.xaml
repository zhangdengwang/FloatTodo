<UserControl x:Class="FloatTodo.App.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:FloatTodo.App.ViewModels"
             MinHeight="520"
             MinWidth="360">
    <UserControl.DataContext>
        <vm:MainViewModel/>
    </UserControl.DataContext>

    <Border Background="#FFF8F8F8"
            CornerRadius="18"
            BorderBrush="#FFE0E0E0"
            BorderThickness="1">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"
                    Margin="0,0,0,12">
                <TextBlock Text="FloatTodo"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#2F2F2F"/>
                <TextBlock Text="轻量悬浮助手"
                           FontSize="12"
                           Foreground="#7A7A7A"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <ItemsControl Grid.Row="1"
                    ItemsSource="{Binding Sections}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,12"
                                Padding="14"
                                Background="White"
                                CornerRadius="12"
                                BorderBrush="#F0F0F0"
                                BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#2F2F2F"/>
                                <TextBlock Text="{Binding Description}"
                                           Margin="0,6,0,0"
                                           FontSize="13"
                                           Foreground="#6F6F6F"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
